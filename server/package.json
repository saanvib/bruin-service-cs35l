{
  "name": "bruin-service-server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --env-file=../.env.development.local --watch index.js",
    "db:migrate": "node --env-file=../.env.development.local scripts/migrate.js",
    "db:seed": "node --env-file=../.env.development.local scripts/seed.js"
  },
  "dependencies": {
    "@descope/node-sdk": "^2.6.0",
    "@neondatabase/serverless": "^1.1.0",
    "cors": "^2.8.6",
    "dotenv": "^16.0.0",
    "express": "^4.18.0"
  }
}
